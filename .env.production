# 운영(prod) 환경 — 배포된 운영 백엔드를 바라봅니다.
# 실제 배포 빌드(Dockerfile build-arg)에서는 GitHub Secret VITE_API_BASE_URL이
# 이 값보다 우선 적용됩니다. 이 파일은 로컬에서 `npm run build`만 실행했을 때의
# 기본값 역할을 합니다.
VITE_API_BASE_URL=https://api.unisize.org
